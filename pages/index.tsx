import React from "react";

import { Layout, ProductCollection } from "@/components";

const Home = () => {
  return (
    <Layout>
      <ProductCollection />
    </Layout>
  );
};

export default Home;
